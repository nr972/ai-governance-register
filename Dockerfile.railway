FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir \
    fastapi uvicorn[standard] sqlalchemy pydantic python-dotenv \
    anthropic reportlab python-docx requests streamlit plotly

COPY . .
RUN chmod +x railway_start.sh

EXPOSE 8000 8501

CMD ["./railway_start.sh"]
