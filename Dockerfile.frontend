FROM python:3.12-slim

WORKDIR /app

RUN pip install --no-cache-dir streamlit requests plotly

COPY agr_frontend/ agr_frontend/

EXPOSE 8501

CMD ["streamlit", "run", "agr_frontend/app.py", "--server.port", "8501", "--server.headless", "true", "--server.address", "0.0.0.0"]
